{
  "name": "mark-wen-space-schema",
  "version": "1.0.0",
  "description": "gRPC schema for Mark Wen Space",
  "scripts": {
    "gen:go": "rm -rf pb && mkdir pb && protoc --go_out=./pb --go-grpc_out=./pb ./proto/*/*.proto ./proto/*/*/*.proto",
    "gen:ts": "rm -rf build && mkdir build && protoc --plugin='protoc-gen-ts=./node_modules/.bin/protoc-gen-ts' --ts_out=./build proto/*/*.proto proto/*/*/*.proto ",
    "nuke": "rm -rf pb build node_modules"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thousight/mark-wen-space-schema.git"
  },
  "keywords": [
    "gRPC"
  ],
  "author": "thousight",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/thousight/mark-wen-space-schema/issues"
  },
  "homepage": "https://github.com/thousight/mark-wen-space-schema#readme",
  "devDependencies": {
    "ts-protoc-gen": "^0.15.0"
  }
}
